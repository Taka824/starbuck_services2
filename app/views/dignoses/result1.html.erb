<h1>コーヒー系</h1>
<div class="result result40 container">
   <div class="row">
      <div class="col-12">
        <div class="row">
          <% @posts = Post.where(dignose_category: "おすすめ1") %>
          <% if @posts.present? %>
            <%= render @posts %>
          <% else %>
            <p><%= t('.no_result') %></p>
          <% end %>
        </div>
      </div>
    </div>
</div>